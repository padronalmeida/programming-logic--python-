salario_atual = float(input('Digite o valor do salário atual (em reais):'))
salario_reajuste_15 = salario_atual * 1.15

print('O salário atual com reajuste de 15% fica R${:.0f}.'.format(salario_reajuste_15))
